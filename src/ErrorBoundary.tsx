import{Component,type ErrorInfo,type ReactNode}from'react';export default class ErrorBoundary extends Component<{children:ReactNode},{failed:boolean}>{state={failed:false};static getDerivedStateFromError(){return{failed:true}}componentDidCatch(e:Error,i:ErrorInfo){console.error(e,i)}render(){return this.state.failed?<main className="fatal"><h1>We couldn’t open this workspace.</h1><p>Your saved project remains on this device. Reload the page to try again.</p><button onClick={()=>location.reload()}>Reload</button></main>:this.props.children}}
